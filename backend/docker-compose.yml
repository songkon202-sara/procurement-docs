services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: procurement
      POSTGRES_PASSWORD: procurement
      POSTGRES_DB: procurement_docs
    ports:
      - '5432:5432'
    volumes:
      - procurement_docs_pgdata:/var/lib/postgresql/data

volumes:
  procurement_docs_pgdata:
